# 原始数据目录

把自行创建或明确获准使用的 `.jsonl` / `.csv` 商品数据放在这里。每条记录至少需要：

- `id`、`title`、`category`、`description`、`price`；
- `source`：数据来源或数据集页面；
- `license`：许可证或授权说明；
- `image_path`：相对当前原始数据文件的本地图片路径（可选）。

不要提交未经许可抓取的商品图片、用户信息、订单数据或评论内容。大型原始数据不应进入 Git，应由下载/生成脚本和版本清单复现。

